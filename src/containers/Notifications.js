import React from "react";

const Notifications = () => {
  // TODO if no new notifications then show <AllDone/>
  // If new notifications, then show the list
  return (
    <div>
      NotificationsNotificationsNotificationsNotifications
      NotificationsNotificationsNotifications NotificationsNotifications
      NotificationsNotificationsNotifications
    </div>
  );
};

export default Notifications;
